<h1>Editing task <%= @task._id %> </h1>

<%= form_for @task, :url => { :action => "update" } do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <br>

    <%= f.label :description %>
    <%= f.text_field :description %>

    <br>

    <%= f.submit %>
<% end %>